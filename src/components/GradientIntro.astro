<section id="gradientSection" class="h-[50vh]" style="--gradient-color-progress:0;">
  <div class="flex flex-col items-center justify-center pt-16 align-middle">
    <div class="flex w-full flex-col items-center justify-center">
      <h2 id="introText" class="absolute mb-4 text-center text-3xl font-bold text-white md:text-left md:text-5xl lg:text-6xl">Meet the <span id="introTextColor" class="bg-[linear-gradient(135deg,_var(--tw-gradient-stops))] from-[#444bf7] to-[#f74444] bg-clip-text text-transparent [-webkit-background-clip:_text]"></span> of scripts.</h2>
    </div>
    <div class="flex w-full flex-col items-center justify-center">
      <p id="introTextGradient" class="absolute text-center !font-sfdisplay text-sm font-semibold text-transparent sm:text-2xl md:text-left lg:text-4xl">Introducing Sirius. <br /> The new undisputed king of scripts.</p>
    </div>
  </div>
</section>

<style>
  #introTextGradient {
    --gradient-transparent-progress: calc(var(--gradient-color-progress) + var(--gradient-feather) * -1%);
    --gradient-feather: 60;
    --gradient-angle: 158deg;
    background-image: linear-gradient(var(--gradient-angle), #cecece var(--gradient-transparent-progress), #cecece00 var(--gradient-color-progress), #cecece00);
    -webkit-background-clip: text;
    -webkit-text-fill-color: #0000;
  }
</style>
